import { makeStyles } from "@material-ui/core";

export default makeStyles(() => ({
  itemGridContainer: {
    display: "grid",
    alignItems: "baseline",
    gridTemplateColumns: "1fr 5fr",
  },
}));
