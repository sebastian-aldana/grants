import { makeStyles } from "@material-ui/core";

export default makeStyles(() => ({
  itemGridContainer: {
    display: "flex",
    alignItems: "baseline",
    padding: "5px",
  },
  cardContent: {
    display: "flex",
  },
}));
