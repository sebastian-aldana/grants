import { makeStyles } from "@material-ui/core";

export default makeStyles(() => ({
  container: {
    margin: "20px",
  },
  tabsContainer: {
    marginTop: "20px",
  },
  tabPanel: {
    boxShadow: "unset",
    backgroundColor: "transparent",
  },
}));
